# 2024-01-15 Git Token 问题

## 问题描述
克隆仓库时遇到权限错误，提示需要输入用户名和密码，但输入后仍然失败。

## 解决方案
1. 登录 GitHub
2. 进入 Settings -> Developer settings -> Personal access tokens
3. 生成新的 token，选择适当的权限
4. 使用 token 作为密码进行克隆：
   ```
   git clone https://username:token@github.com/username/repository.git
   ```